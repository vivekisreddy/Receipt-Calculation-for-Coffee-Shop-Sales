# include <stdio.h>

// Functions prototypes 

float overall_revenue(FILE *file); // function prototype for overall_revenue function 

float balance(FILE *file); // function prototype for calculating balance function 

int tally(FILE *file, int item); // function prototype for calculating item sales, function has two arguments because it takes the inout file and input item by the user 